const AboutUs = () => {
	return (
		<div>
			<h1>About us page</h1>
			<p>The problem is, instructor is using Next.js version 12 and when I create a next.js now it will create with 13.1 version but there is drastic differences. How can I overcome this ? and is it possible to use yarn while doing it from terminal.</p>
		</div>
	)
}

export default AboutUs
